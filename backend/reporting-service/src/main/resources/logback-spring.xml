<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback config for reporting-service.

  Two profiles:
    - default (docker/prod)  → JSON to stdout with traceId/spanId from MDC.
                              A log aggregator (Loki, Datadog) can slurp this
                              without regex parsing and pivot to Jaeger by
                              clicking a traceId.
    - local                  → human pattern for `mvn spring-boot:run` UX.

  MDC keys "traceId" and "spanId" are populated automatically by
  micrometer-tracing-bridge-otel; we only need to reference them.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="!local">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <fieldName>@timestamp</fieldName>
                    </timestamp>
                    <version>
                        <fieldName>@version</fieldName>
                    </version>
                    <pattern>
                        <pattern>
                            {
                              "service": "reporting-service",
                              "level": "%level",
                              "logger": "%logger{40}",
                              "thread": "%thread",
                              "traceId": "%mdc{traceId:-}",
                              "spanId": "%mdc{spanId:-}"
                            }
                        </pattern>
                    </pattern>
                    <message/>
                    <mdc>
                        <excludeMdcKeyName>traceId</excludeMdcKeyName>
                        <excludeMdcKeyName>spanId</excludeMdcKeyName>
                    </mdc>
                    <stackTrace>
                        <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                            <maxDepthPerThrowable>30</maxDepthPerThrowable>
                            <maxLength>4096</maxLength>
                            <rootCauseFirst>true</rootCauseFirst>
                        </throwableConverter>
                    </stackTrace>
                </providers>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

    <springProfile name="local">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%X{traceId:-},%X{spanId:-}] %logger{40} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
