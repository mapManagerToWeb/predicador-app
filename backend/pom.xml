<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>com.predicador</groupId>
    <artifactId>predicador-backend</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>Predicador Backend</name>
    <description>Backend microservices for Predicador PWA</description>

    <properties>
        <java.version>25</java.version>
        <spring-cloud.version>2025.1.0</spring-cloud.version>
        <springdoc.version>3.1.0</springdoc.version>
        <testcontainers.version>1.20.1</testcontainers.version>
        <netty.version>4.2.16.Final</netty.version>
        <tomcat.version>11.0.24</tomcat.version>
        <!--
            Observability stack. OpenTelemetry Spring Boot starter uses the
            OTel BOM to align every otel-* artifact on the same generation.
            logstash-logback-encoder emits JSON logs with MDC (traceId/spanId
            injected by Micrometer Tracing) so Grafana Loki / any log collector
            can correlate logs with traces without regex parsing.
        -->
        <opentelemetry.version>1.64.0</opentelemetry.version>
        <opentelemetry-instrumentation.version>2.30.0</opentelemetry-instrumentation.version>
        <logstash-logback.version>8.0</logstash-logback.version>
        <!--
            Override for known CVEs in httpcore5 (CVE-2026-54399,
            CVE-2026-54428, both HIGH) affecting 5.3.6 and earlier 5.4.x.
            Spring Boot 4.0.7 manages httpcore5 via ${httpcore5.version}.
        -->
        <httpcore5.version>5.4.3</httpcore5.version>
        <!--
            Override for known CVEs in amqp-client (CVE-2026-63337,
            CVE-2026-69219, CVE-2026-69220, all HIGH) affecting 5.27.x.
            Spring Boot 4.0.7 manages amqp-client via
            ${rabbit-amqp-client.version}.
        -->
        <rabbit-amqp-client.version>5.33.1</rabbit-amqp-client.version>
    </properties>

    <modules>
        <module>shared</module>
        <module>config-server</module>
        <module>discovery-server</module>
        <module>api-gateway</module>
        <module>territory-service</module>
        <module>reporting-service</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry.instrumentation</groupId>
                <artifactId>opentelemetry-instrumentation-bom</artifactId>
                <version>${opentelemetry-instrumentation.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!--
                Transitive dependency overrides for known CVEs (CVE-2026-54291
                in pgjdbc, CVE-2025-14813 in Bouncy Castle) that the managed
                versions shipped by Spring Boot / Spring Cloud do not yet fix.
            -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>42.7.12</version>
            </dependency>
            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>1.81.1</version>
            </dependency>
            <!-- Spring Cloud Config Server 5.0.0 has CVEs (CVE-2026-40982 CRITICAL, CVE-2026-22739 HIGH, CVE-2026-40981 HIGH, CVE-2026-41002 HIGH). Fixed in 5.0.3. -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-config-server</artifactId>
                <version>5.0.3</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins/>
    </build>

    <profiles>
        <profile>
            <id>coverage</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>0.8.14</version>
                        <executions>
                            <execution>
                                <id>prepare-agent</id>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>report</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>check</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>check</goal>
                                </goals>
                                <configuration>
                                    <rules>
                                        <rule>
                                            <element>BUNDLE</element>
                                            <limits>
                                                <limit>
                                                    <counter>LINE</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.40</minimum>
                                                </limit>
                                                <limit>
                                                    <counter>INSTRUCTION</counter>
                                                    <value>COVEREDRATIO</value>
                                                    <minimum>0.40</minimum>
                                                </limit>
                                            </limits>
                                        </rule>
                                    </rules>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
