/**
 * Shared authentication primitives: HMAC-based session tokens used by the
 * gateway, reporting-service and territory-service to authenticate requests
 * without requiring an external identity provider.
 */
@NullMarked
package com.predicador.shared.security;

import org.jspecify.annotations.NullMarked;
