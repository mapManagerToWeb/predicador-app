spring:
  application:
    name: config-server
  profiles:
    active: native

server:
  port: 8888

spring.cloud.config.server.native:
  search-locations: classpath:/config

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
